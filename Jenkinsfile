pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                mvn 'mvn package'   // Replace 'mvn package' with your build command if different
            }
        }
    }
}